2.3.8 About how many compares will Quick.sort() make when sorting an array of
N items that are all equal?

Because partition stops at the equal keys,
if all keys are equal, the partitioning item will split the list in half each time
This should converge to NlogN compares
